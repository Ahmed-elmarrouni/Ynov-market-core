apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: campus-market-dev
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 4100
      targetPort: 4100
  type: ClusterIP
